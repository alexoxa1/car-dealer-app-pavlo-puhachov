import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: "export",
  images: {
    unoptimized: true,
  },
  basePath: "/alexoxa1/car-dealer-app-pavlo-puhachov",
};

export default nextConfig;
